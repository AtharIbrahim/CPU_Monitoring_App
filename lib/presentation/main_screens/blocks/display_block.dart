import 'package:flutter/material.dart';

class DisplayBlock extends StatelessWidget {
  const DisplayBlock({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
